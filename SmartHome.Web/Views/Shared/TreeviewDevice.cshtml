@model IEnumerable<SmartHome.Entity.DeviceInfoEntity>
@GetTreeView(Model, Model.FirstOrDefault().ParentId)

@helper GetTreeView(IEnumerable<SmartHome.Entity.DeviceInfoEntity> siteMenu, int parentID)
{
foreach (var i in siteMenu.Where(a => a.ParentId.Equals(parentID)))
{
        <li>
            @{var submenu = siteMenu.Where(a => a.ParentId.Equals(i.SequenceId)).Count();}
            @if (submenu > 0)
            {
                <span class="collapse collapsible">&nbsp;</span>
            }
            else
            {
                <span style="width:15px; display:inline-block">&nbsp;</span>
            }
            <span>
                <a href="#">@i.DisplayName</a>
            </span>
            @if (submenu > 0)
            {
                <ul>
                    @GetTreeView(siteMenu, i.SequenceId)
                    @* Recursive  Call for Populate Sub items here*@
                </ul>
            }
        </li>
                }
}
