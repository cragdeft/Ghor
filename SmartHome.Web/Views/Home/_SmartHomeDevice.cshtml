@model List<SmartHome.Model.Models.Room>


    @foreach (var nextRoom in Model)
    {
        @*@Html.Raw("<div id="+ @nextRoom.Name.Replace(" ", "_") + ">")*@

        
        <div id="@nextRoom.Name.Replace(" ", "_")">
            <span class="text-left">
                <h4>@nextRoom.Name</h4>
                
            </span>
        </div>
        <hr />
        @Html.Raw("<div class=row>")
        foreach (var nextDevice in nextRoom.SmartDevices)
        {
            switch (nextDevice.DeviceType)
            {
                case DeviceType.SmartSwitch6g:
                    @*@Html.SmartSwitch6g(nextDevice)*@
                    @Html.Partial("_SmartHomeDeviceSwitch", nextDevice)
                    break;
                case DeviceType.SmartRainbow12:
                    @Html.Partial("_SmartHomeDeviceRGBW")
                    break;
                case DeviceType.CurtainV:

                    break;
                case DeviceType.Camera:
                    break;
                case DeviceType.SmartRouter:
                    @Html.Partial("_SmartHomeDeviceRouter")
                    break;

            }
        }

        @Html.Raw("</div>")

    }



